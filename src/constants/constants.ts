/**
 * An object representing the size of the grid.
 * @typedef {Object} GridSize
 * @property {number} row - The number of rows in the grid.
 * @property {number} col - The number of columns in the grid.
 */

/** An object defining the size of the grid. */
export const GRID_SIZE = { row: 8, col: 8 };

/** The number of mines defined as a constant. */
export const NUM_MINES = 10;

/** The number of double tapped delay as a constant. */
export const TAP_DELAY = 250;
